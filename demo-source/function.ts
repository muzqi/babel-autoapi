
/**
 * sayHi
 * @param {string} name this is name
 * @param {number} age  this is age
 * @param {boolean} b 这是一个布尔值
 * @author muzi
 * @example sayHi('muzi'); // -> muzi
 */
const sayHi = (name) => {
  console.log(name);
}
